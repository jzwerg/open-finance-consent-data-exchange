# Open Finance Consent & Data Exchange Gateway — copy to `.env`.
# No external secrets needed: the auth server generates signing keys at startup.

POSTGRES_USER=openfinance
POSTGRES_PASSWORD=openfinance
POSTGRES_DB=openfinance
FAPI_KEYS_DIR=/keys
